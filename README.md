# Movie Night API

## Hosting

Boot the VM using Vagrant, and log in:

    vagrant up
    vagrant ssh

## Starting the API

Install the dependencies:

    npm install

Start the Express server:

    npm start

## Development Tooling

    grunt watch
    